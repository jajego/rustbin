- limit size of headers
- limit # per bin
- do not expire active connections
- uuid parsing
- cors
- offer in memory option (As opposed to default sqlite)
- when using sqlite option, prevent a user from viewing another users' bins
